module SemigroupExercises where

import Test.QuickCheck (Gen, Arbitrary, arbitrary, quickCheck, elements, frequency)
-- import Data.Semigroup (Semigroup, (<>))

-- Skipped them. Were the same as the Semigroup exercises
-- just had to add mempty. Just don't have the time

main :: IO ()
main = return ()
